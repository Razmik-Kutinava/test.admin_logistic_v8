# 🔧 Решение ошибок на http://localhost:3001/

## ❌ Обнаруженные проблемы:

1. **Миграция не применена** - таблицы отсутствуют в базе данных
2. **Конфликт портов** - frontend и backend на одном порту

## ✅ Решение:

### Шаг 1: Применить миграцию (ОБЯЗАТЕЛЬНО!)

1. Откройте файл `backend/APPLY_MIGRATION.sql`
2. Скопируйте ВСЁ содержимое (Ctrl+A, Ctrl+C)
3. Откройте Supabase Dashboard:
   - https://app.supabase.com
   - Выберите проект
   - SQL Editor → New Query
4. Вставьте SQL и нажмите **Run** (Ctrl+Enter)

### Шаг 2: Исправить конфликт портов

**Вариант A: Запустить frontend на порту 3000 (рекомендуется)**

Убедитесь, что порт 3000 свободен, и перезапустите frontend:
```bash
cd frontend
npm run dev
```

Frontend должен запуститься на http://localhost:3000
Backend работает на http://localhost:3001

**Вариант B: Изменить порт backend**

Если порт 3000 занят, измените порт backend в `.env`:
```
PORT=3002
```

И обновите proxy в `frontend/vite.config.ts`:
```typescript
proxy: {
  '/api': {
    target: 'http://localhost:3002',  // Изменить на 3002
    changeOrigin: true,
  },
}
```

### Шаг 3: Перезапустить сервисы

1. Остановите все запущенные процессы (Ctrl+C)
2. Запустите backend:
   ```bash
   cd backend
   npm run start:dev
   ```
3. Запустите frontend:
   ```bash
   cd frontend
   npm run dev
   ```

### Шаг 4: Проверить

1. Откройте http://localhost:3000 (frontend)
2. Backend API: http://localhost:3001/api/v1
3. Ошибки должны исчезнуть

## 📋 Проверка миграции

Выполните в Supabase SQL Editor:

```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
ORDER BY table_name;
```

Должны быть таблицы: deliveries, districts, drivers, orders, system_logs, warehouses, zones

